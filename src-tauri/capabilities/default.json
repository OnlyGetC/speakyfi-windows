{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Speakyfi window",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "shell:default",
    "fs:default",
    "dialog:default",
    "notification:default",
    "global-shortcut:default"
  ]
}
